from setuptools import setup

setup(
    name                 = "electron_density",
    version              = "2020.0",
    packages             = ['electron_density'],
    scripts              = ['electron_density/density_calculator_intelHD.py']
    )
